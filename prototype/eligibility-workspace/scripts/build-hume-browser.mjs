import { build } from "esbuild";

await build({
  entryPoints: ["hume-sdk-browser-entry.js"],
  outfile: "vendor-hume-evi.js",
  bundle: true,
  format: "iife",
  platform: "browser",
  target: ["safari15", "chrome100"],
  minify: true,
  sourcemap: false,
  legalComments: "none",
  banner: { js: "/* Hume browser SDK 0.16.0 — generated by npm run build:hume */" },
});
